apiVersion: v1
kind: Service
metadata:
  name: devops-fastapi-service
spec:
  selector:
    app: devops-fastapi
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP

