apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-fastapi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devops-fastapi
  template:
    metadata:
      labels:
        app: devops-fastapi
    spec:
      containers:
        - name: api
          image: ghcr.io/jakubfurmanskijf/devops-fastapi:1.0
          ports:
            - containerPort: 8000

